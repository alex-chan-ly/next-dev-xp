// Import React
import React from "react";

// Import Spectacle Core tags
import {
  BlockQuote,
  Cite,
  Deck,
  Heading,
  ListItem,
  List,
  Quote,
  Slide,
  Text
} from "spectacle";
import WorkshopHome from '../../components/WorkshopHome';
export default [
<WorkshopHome
    title={`Security - Access Control Lists`}
    description={`Enabled intra-mesh authorization via access control lists, enforced via Mixer policy checks`}
    difficulty={5}
    minutes={30}
    number={"12"} />
]