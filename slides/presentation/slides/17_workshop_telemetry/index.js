// Import React
import React from "react";

// Import Spectacle Core tags
import {
  BlockQuote,
  Cite,
  Deck,
  Heading,
  ListItem,
  List,
  Quote,
  Slide,
  Text
} from "spectacle";
import WorkshopHome from '../../components/WorkshopHome';
export default [
<WorkshopHome
    title={`Telemetry`}
    description={`Deep dive into observability tooling and how these metrics are being collecting to populate our given observability tooling. Exploring the Mixer Adapter model`}
    difficulty={6}
    minutes={70}
    number={"13"} />
]