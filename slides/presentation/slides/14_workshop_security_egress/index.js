// Import React
import React from "react";

// Import Spectacle Core tags
import {
  BlockQuote,
  Cite,
  Deck,
  Heading,
  ListItem,
  List,
  Quote,
  Slide,
  Text
} from "spectacle";
import WorkshopHome from '../../components/WorkshopHome';
export default [
<WorkshopHome
    title={`Security - Egressgateway mTLS`}
    description={`Originating a mTLS connecting within the service mesh. Simplifying the management of client TLS certificates`}
    difficulty={4}
    minutes={30}
    number={"10"} />,
]